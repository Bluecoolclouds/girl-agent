> 🎃:
Проблема ясна — в логах видно что frontend в PM2 остановлен (stopped). Бэкенд (girl-agent-src) обновился, а веб-панель не запущена.
Скажи Лапе:
Фронтенд (веб-панель) у тебя остановлен в PM2. Нужно:
Установить pnpm: npm install -g pnpmПоставить зависимости в корне: cd /root/girl-agent && pnpm installСобрать фронтенд: pnpm --filter @workspace/girl-agent run buildПерезапустить frontend в PM2: pm2 restart frontend (или pm2 start frontend если остановлен)Показать pm2 list после этогоЕсли pm2-конфига для frontend нет — тогда:
pm2 start "pnpm --filter @workspace/girl-agent run dev" --name frontend

> Lapa:
Проверил. Сайт обновился технически, но визуально ты мог не увидеть изменений, потому что последний коммит не меняет главную страницу/UI.

Что выяснил:
- домен http://aigirl.artificecheat.ru/ отвечает с того же процесса, что и локальный 127.0.0.1:3000
- nginx проксирует правильно
- girl-agent сейчас online
- текущий коммит на сервере: 1fd4fc3
- health-check живой: {"ok":true,...}

Почему “не видно обновления”:
Последний коммит с GitHub меняет только один файл:
- girl-agent-src/src/engine/runtime.ts

Сообщение коммита:
- Ensure blocked users are properly marked and ignored by the system

То есть это логика движка, а не внешний вид сайта.  
Поэтому страница aigirl.artificecheat.ru может выглядеть ровно так же, хотя код уже новый.

Итог:
- ✅ проект обновлён до последней версии
- ✅ процесс перезапущен
- ✅ домен смотрит на актуальный процесс
- ❗ изменений в UI в последнем коммите просто нет

Если хочешь, я могу дальше:
1. показать git diff HEAD~1 HEAD по runtime.ts
2. проверить, какой именно экран ты ожидал увидеть обновлённым
3. принудительно полностью перезапустить стек ещё жёстче: pm2 delete girl-agent && заново стартовать  
4. сравнить HTML главной страницы сервера с тем, что ожидается из репозитория

> Lapa:
📋 План:
1. Подключиться к серверу и проверить текущий PM2/структуру проекта
2. Установить pnpm, если его нет
3. Установить зависимости и собрать фронтенд
4. Запустить/перезапустить frontend в PM2
5. Показать статус и проверить, почему сайт не обновлялся

Выполняю.
